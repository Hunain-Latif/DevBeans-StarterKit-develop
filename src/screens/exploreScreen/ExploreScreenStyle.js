import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1
    },

    map: {
        // ...StyleSheet.absoluteFillObject,
        width: '100%',
        height: 250,
        // marginTop: 50

    },

})